import * as React from 'react'
import { CircularProgress, DivStyled } from '.'

export const Loading = () => {
  return (
        <DivStyled>
            <CircularProgress />
        </DivStyled>
  )
}
